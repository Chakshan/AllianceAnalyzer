public class Analysis {

    // Create robots here and add to array
    // Example
    RechargeBot supporter = new RechargeBot(false, true, true, true, false, false);

    RechargeBot[] testBots = new RechargeBot[] {supporter};

    // Create list of permutations
    // Feed into Match
    // Counter for each type of robot
    // Increment if that type of robot wins a match
    
}